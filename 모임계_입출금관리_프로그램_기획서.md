# 모임계(친구) 입출금 관리 프로그램 - 바이브코딩 프롬프트 기획서

2026-09-17

## 1. 개요

**목적**
현재 엑셀로 관리하고 있는 친구들 모임계(회비)의 입금·출금 내역을 웹/모바일 프로그램으로 옮겨, 매달 회비 입금 확인, 지출 등록, 잔액 관리를 자동화하고 친구들과 실시간으로 공유한다.

**현재 방식의 한계 (엑셀)**
- 친구별 입금액/일자를 매달 수동 입력 → 오탈자, 빠뜨림 발생
- 영수증은 따로 사진으로만 보관, 지출 내역과 연결되지 않음
- 잔액은 수식으로 계산하지만, 시트를 직접 열어야만 확인 가능 → 친구들 공유가 불편(파일 전달/캡처)
- 은행이자 입력도 수동

**프로그램화의 목표**
- 입금/출금 등록을 간단한 화면(모바일 우선)에서 처리
- 출금 시 영수증을 사진으로 찍어 바로 첨부(스캔 기능)
- 잔액은 등록 즉시 자동 계산
- 친구들에게 링크 하나로 공유해서 각자 조회 가능(계좌 앱처럼)

> 이 문서는 코드가 아니라, 바이브코딩(AI 코딩 도구)에 넘길 프롬프트를 정리하는 것이 목적입니다. 실제 구현은 이 문서 마지막의 "최종 프롬프트"를 Claude Code 등에 붙여넣어 진행합니다.

## 2. 사용자 및 공유 방식

**사용자**
- 총무: 입금/출금 등록, 친구 관리(등록/수정/삭제), 회비 기본값 설정
- 친구(회원): 입금/출금 등록 불가, 조회만 가능, 공지사항은 자유롭게 작성 가능

**공유 방식**
- 웹링크로 충분 (로그인/앱 설치 불필요, 카카오톡으로 링크만 전달)

## 3. 핵심 기능 정의

### 3-1. 입금내역 등록
- 회비 일괄 등록: 여러 친구에게 동일 금액을 한 번에 등록
- 일자별 여러 건 등록 가능
- 입금자 다중 선택: 같은 날 여러 친구를 체크해서 한 번에 등록
- 회비 기본값 자동 입력: 금액란에 기본값(초기 20,000원)이 자동으로 채워지고, 필요 시 수정 가능
- 기본값은 설정 화면에서 총무가 언제든 변경 가능

### 3-2. 출금내역 등록 (영수증 포함)
- 지출내용, 금액, 날짜를 총무가 직접 입력
- 영수증 이미지 첨부(촬영 또는 갤러리 선택)
- 첨부 즉시 미리보기로 확인 후 저장
- OCR(영수증 금액 자동 인식)은 이번 버전 제외, 추후 검토 항목으로 분리

### 3-3. 잔액 자동계산
- 잔액 = 이전 잔액 + 이번 달 입금합계(회비+이자) - 이번 달 지출합계
- 내역을 등록/수정/삭제할 때마다 잔액이 자동으로 갱신되어야 함
- 월별 요약(입금합계, 지출합계, 잔액)과 전체 내역(리스트)을 함께 볼 수 있어야 함

### 3-4. 조회/공유
- 친구들은 월별 입금·출금 내역과 현재 잔액을 조회만 가능
- 특정 기간(예: 연도별) 필터링

### 3-5. 공지사항 (신규)
- 회원 누구나 자유롭게 작성 가능 (권한 제한 없음)
- 중요/일반 태그 구분
- 목록 조회 + 전체보기
- 작성은 대시보드에서 모달(팝업)로 처리

### 3-6. 친구 관리 (신규)
- 총무: 친구(회원) 등록/수정/삭제
- 친구(일반 회원): 목록 조회만 가능, 등록/수정 불가

## 4. 데이터 구조 설계 (첨부 엑셀 기반)

첨부해주신 엑셀 시트를 보면 크게 **회원 마스터 / 입금내역 / 출금내역 / 월별 요약(잔액)** 4가지로 나눌 수 있습니다.

### members (친구/회원)
| 필드 | 설명 |
|---|---|
| id | 회원 고유번호 |
| name | 이름 |

### deposits (입금내역)
| 필드 | 설명 |
|---|---|
| id | 고유번호 |
| member_id | 입금한 친구 |
| amount | 입금액 |
| paid_date | 입금일자 |
| type | 구분(회비 / 은행이자 등) |
| month | 귀속 월(예: 2026-09) |

### withdrawals (출금내역)
| 필드 | 설명 |
|---|---|
| id | 고유번호 |
| content | 지출내용(예: 펜션비) |
| amount | 지출금액 |
| spent_date | 지출일자 |
| receipt_image | 영수증 이미지 파일(URL/경로) |
| month | 귀속 월 |

### monthly_summary (월별 요약, 조회용 — 실제 값은 위 두 테이블에서 계산)
| 필드 | 설명 |
|---|---|
| month | 해당 월 |
| deposit_total | 입금합계금액 |
| withdrawal_total | 지출합계 |
| balance | 잔액(전월 잔액 + 입금합계 - 지출합계) |

### notices (공지사항, 신규)
| 필드 | 설명 |
|---|---|
| id | 고유번호 |
| author | 작성자(회원, 누구나) |
| title | 제목 |
| content | 내용 |
| tag | 중요/일반 |
| created_date | 작성일 |

> 잔액은 별도로 저장하지 않고, 매번 "이전 잔액 + 이번 달 입금 - 이번 달 지출"로 계산해서 보여주는 방식을 권장합니다(수동 오류 방지).

## 5. 화면(UI) 구성

기본 구조: 대시보드를 중심으로 이동. 상단 메뉴(대시보드 / 내역보기 / 친구 관리 / 설정) 클릭 시 해당 화면으로 이동하고, **입금 등록·출금 등록·공지 작성은 화면 이동 없이 대시보드 위에 모달(팝업창)로 처리**한다.

**① 메인 대시보드** *(기본 화면)*
- 상단: 로고/앱명, 내비게이션(대시보드/내역보기/친구 관리/설정), 사용자 정보
- 환영 인사 + 문구 카드
- 요약 카드 3개: 현재 잔액 / 이번 달 입금합계(전월대비%) / 이번 달 지출합계(전월대비%)
- "입금 등록" 버튼 → **입금 등록 모달** 오픈
- "출금 등록" 버튼 → **출금 등록 모달** 오픈
- 좌측: 공지사항 목록(요약) + "공지 작성" 버튼 → **공지 작성 모달** 오픈
- 우측: 최근 내역 목록(요약) + "전체 내역 보기" 클릭 → **내역보기 화면**으로 이동

**② 입금 등록 모달**
- 입금자 다중 선택, 금액(기본값 20,000원 자동 입력, 수정 가능), 날짜
- 일괄 등록 지원, 저장 후 모달 닫히고 대시보드 요약 자동 갱신

**③ 출금 등록 모달**
- 지출내용, 금액, 날짜 입력 + 영수증 촬영/첨부 + 미리보기
- 저장 후 모달 닫히고 대시보드 요약 자동 갱신

**④ 공지 작성 모달**
- 제목, 내용, 중요/일반 태그 선택
- 회원 누구나 작성 가능, 저장 후 모달 닫히고 공지 목록 자동 갱신

**⑤ 내역보기 화면**
- 월별 입금/출금 내역 리스트, 영수증 썸네일(클릭 시 확대)

**⑥ 공지사항 화면**
- 공지 전체 목록(중요/일반 태그), 상세 보기

**⑦ 친구 관리 화면**
- 총무: 친구 추가/수정/삭제
- 친구(일반 회원): 목록만 조회

**⑧ 설정 화면**
- 회비 기본값 설정(현재 20,000원, 총무만 변경 가능)
- 관리자 핀번호 설정

## 6. 기술 스택 제안 (비개발자 바이브코딩용)

- **프레임워크**: Next.js (모바일에서도 잘 보이는 반응형 웹)
- **데이터베이스/저장소**: Supabase (테이블 관리 + 영수증 이미지는 Supabase Storage에 저장, 무료 티어로 시작 가능)
- **배포**: Vercel (배포 후 나오는 URL을 친구들에게 카카오톡으로 공유)
- **영수증 스캔**: 별도 스캐너 앱 없이, 모바일 브라우저의 `<input type="file" accept="image/*" capture="environment">`로 카메라 바로 실행 → 촬영 즉시 첨부(추가 라이브러리 불필요)

이 조합은 코드를 몰라도 Claude Code 같은 AI 코딩 도구에 프롬프트만 넘기면 바로 실행/배포까지 진행할 수 있는 조합입니다.

## 7. 바이브코딩용 최종 프롬프트

아래 내용을 그대로 Claude Code(또는 다른 AI 코딩 도구)에 붙여넣어 시작하시면 됩니다.

```
친구들과 함께 운영하는 "모임계(회비)" 입출금 관리 웹앱을 만들어줘.

[목적]
친구들이 매달 내는 회비(입금)와 모임에서 쓴 지출(출금) 내역을 등록하고,
현재 잔액을 자동으로 계산해서 친구들과 공유하는 앱이야.

[기술 스택]
- Next.js (반응형, 모바일 우선)
- Supabase (데이터베이스 + 이미지 저장소, 인증은 사용하지 않음)
- Vercel에 배포해서 링크로 공유할 것

[사용자 및 권한]
- 총무(관리자): 입금/출금 등록, 친구 관리(등록/수정/삭제), 회비 기본값 설정 — 모두 가능
- 친구(일반 회원): 입금/출금 등록 불가, 모든 내역 조회만 가능, 공지사항은 누구나 자유롭게 작성 가능
- 로그인 없이 공유 링크로 접속. 총무 전용 기능(등록/설정)은 간단한 핀번호로 구분

[데이터 구조]
1. members(친구/회원): id, name
2. deposits(입금내역): id, member_id, amount, paid_date, type(회비/은행이자), month
3. withdrawals(출금내역): id, content, amount, spent_date, receipt_image_url, month
4. notices(공지사항): id, author, title, content, tag(중요/일반), created_date
※ 잔액은 저장하지 않고, "이전 잔액 + 이번 달 입금합계 - 이번 달 지출합계"로 매번 계산해서 보여줘.
※ 회비 기본값(초기 20,000원)을 설정 테이블/값으로 두고, 입금 등록 시 금액란에 자동으로 채워지도록 해줘. 이 기본값은 설정 화면에서 총무가 언제든 바꿀 수 있어야 해.

[내비게이션 구조]
- 모든 화면 이동은 메인 대시보드를 중심으로 이루어져.
- 상단 메뉴(대시보드 / 내역보기 / 친구 관리 / 설정)를 클릭하면 해당 화면으로 "이동"해줘.
- 입금 등록, 출금 등록, 공지 작성은 화면 이동 없이 대시보드 위에 "모달(팝업창)"로 띄워줘.

[화면 구성]
1. 메인 대시보드
   - 현재 잔액, 이번 달 입금합계/지출합계(전월 대비 증감률 포함)
   - "입금 등록" / "출금 등록" 버튼 → 각각 모달 오픈
   - 공지사항 목록(요약) + "공지 작성" 버튼 → 모달 오픈
   - 최근 내역 목록(요약) + "전체 내역 보기" → 내역보기 화면으로 이동

2. 입금 등록 모달
   - 친구 다중 선택(체크박스), 금액(기본값 자동 입력, 수정 가능), 날짜
   - 여러 친구를 한 번에 일괄 등록 가능

3. 출금 등록 모달
   - 지출내용, 금액, 날짜 입력
   - <input type="file" accept="image/*" capture="environment"> 로 영수증을 카메라로 바로 촬영하거나 갤러리에서 선택
   - 첨부한 이미지는 Supabase Storage에 업로드하고 저장 전 미리보기로 보여줘

4. 공지 작성 모달
   - 제목, 내용, 중요/일반 태그 선택
   - 회원 누구나 작성 가능 (권한 제한 없음)

5. 내역보기 화면
   - 월별로 입금/출금 내역을 표로 보여주고, 출금 항목은 영수증 썸네일을 표시, 클릭하면 원본 이미지를 확대해서 볼 수 있게 해줘.

6. 공지사항 화면
   - 공지 전체 목록(중요/일반 태그 구분) + 상세 보기

7. 친구 관리 화면
   - 총무: 친구 추가/수정/삭제
   - 일반 회원: 목록만 조회

8. 설정 화면
   - 회비 기본값 설정(현재 20,000원)
   - 관리자 핀번호 설정

[우선 구현 순서]
1) Supabase 프로젝트/테이블 세팅 (members, deposits, withdrawals, notices)
2) 입금 등록 모달 + 리스트 조회
3) 출금 등록 모달 + 영수증 첨부/미리보기
4) 잔액 자동계산 + 대시보드 요약
5) 공지사항 모달 + 목록 화면
6) 친구 관리 화면 (총무 권한 구분)
7) 회비 기본값 설정 화면 + 핀번호 보호
8) Vercel 배포 + 공유 링크 테스트

비개발자라 각 단계마다 어떤 파일을 만들고 무엇을 하는 건지 간단히 설명해주면서 진행해줘.
```
